import { ToStringable } from '~/utils/core'

export type CSSVars = {
  ghostButton_bg?: ToStringable
  ghostButton_bg__hovered?: ToStringable
  ghostButton_bg__active?: ToStringable
  ghostButton_color?: ToStringable
}
