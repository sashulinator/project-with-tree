import { ToStringable } from '~/utils/core'

export type CSSVars = {
  primaryButton_bg?: ToStringable
  primaryButton_bg__hovered?: ToStringable
  primaryButton_bg__active?: ToStringable
  primaryButton_color?: ToStringable
}
