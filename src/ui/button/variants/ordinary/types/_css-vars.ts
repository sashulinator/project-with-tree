import { ToStringable } from '~/utils/core'

export type CSSVars = {
  ordinaryButton_bg?: ToStringable
  ordinaryButton_bg__hovered?: ToStringable
  ordinaryButton_bg__active?: ToStringable
  ordinaryButton_bg_color?: ToStringable
}
