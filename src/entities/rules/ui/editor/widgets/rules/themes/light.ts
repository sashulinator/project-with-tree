import { common } from './common'
import { CSSVars } from './types'

export const light: CSSVars = {
  ...common,
  editor_bg: '#fff',
  editor_boxShadow: '0px 0px 8px 0px rgba(0, 0, 0, 0.1)',
  editorItem_bg: '#f4f4f4',
  mentionItem_bg: '#63B5C2',
}
